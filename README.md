# RETIRED — lowercase `jaypv-os`

This repository is retained only for historical readback and migration evidence. It is not an active JPV runtime, deployment source, production authority, or feature-development target.

Canonical JPV operating-system authority is:

`jaypVLabs/JPV-OS`

Do not deploy from this repository. Do not add new runtime features here. Do not treat dependency-update, provider-status, or merge activity in this repository as evidence about the canonical JPV-OS runtime.

The historical source remains preserved so prior decisions and artifacts can be reconstructed when needed. Repository lifecycle is machine-readable in `.jpv/repository-status.json`.
