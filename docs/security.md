# Security & Configuration

Sensitive configuration is handled via environment variables and is not included in this repository.

- `.env` is gitignored
- `.env.example` is provided for safe configuration
- No tokens, API keys, or secrets are committed
